// Firebase Web API keys identify a Firebase project; they are not secret
// credentials. Copy this project's Web API key from:
// Firebase Console → Project settings → General → Your apps.
window.PLANK_FIREBASE_CONFIG = Object.freeze({
  apiKey: 'REPLACE_WITH_FIREBASE_WEB_API_KEY'
});
